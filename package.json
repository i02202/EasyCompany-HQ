{
  "name": "easycompany-hq",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n vite,server -c cyan,magenta \"vite\" \"miniverse --no-browser\"",
    "build": "vite build",
    "preview": "vite preview",
    "server": "miniverse",
    "generate": "miniverse-generate"
  },
  "dependencies": {
    "@miniverse/core": "^0.2.6",
    "@miniverse/server": "^0.2.8",
    "@pixellab-code/pixellab": "^1.0.2",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@miniverse/generate": "^0.2.0",
    "concurrently": "^9.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "vitest": "^4.1.2"
  }
}
